# GAS のコード（LINEトークンを含むため公開リポジトリに置かない）
*.gs

# 図面の元画像（アプリは webp のみ参照。約200MBあるためアップしない）
map_*.png

# 開発用ツール
coord_picker.html

# ローカル確認用サーバー設定
.claude/

# 会社ごとのデータと組み立て結果（お客さんのデータを公開リポジトリに上げない）
customers/
dist/
